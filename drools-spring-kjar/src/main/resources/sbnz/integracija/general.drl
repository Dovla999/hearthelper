package sbnz.integracija;

import sbnz.integracija.backend.facts.*;


rule "Classify Item - Low Range"
    when
        $card: Card( cost <= 5, category == Hero.HUNTER )
    then
        $card.setCost(7);
        update($card);
end